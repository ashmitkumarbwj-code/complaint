# Phase 4 — Critical Fixes Task Tracker

## Fix Order (Approved)

- [x] **F1** — Add `logger` import to `authController.js`
- [x] **F2** — Fix `this.generateTokens` in `authService.js`
- [x] **F3** — Fix Socket.io anonymous tenant leak
- [x] **F4** — Protect `/api/health/info` endpoint
- [x] **F5** — OTP migration SQL + re-enable IP rate limiting in `otpService.js`
- [x] **F6** — Replace `Math.random()` with `crypto.randomInt()` in OTP generation
