const fs = require("fs"); const path = require("path"); function injectFavicon(dir) { const files = fs.readdirSync(dir); for (const file of files) { const fullPath = path.join(dir, file); if (fs.statSync(fullPath).isDirectory()) { injectFavicon(fullPath); } else if (fullPath.endsWith(".html")) { let content = fs.readFileSync(fullPath, "utf8"); if (!content.includes("favicon.jpg")) { content = content.replace("</head>", "    <link rel=\"icon\" type=\"image/jpeg\" href=\"/images/favicon.jpg\">\n</head>"); fs.writeFileSync(fullPath, content); console.log("Updated", fullPath); } } } } injectFavicon(path.join(__dirname, "public")); console.log("Done");
